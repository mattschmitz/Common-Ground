{
  "name": "Common Ground",
  "description": "finds places for users coming from different locations to meet",
  "scripts": {
  },
  "env": {
    "YELP_API_KEY": {
      "required": true
    },
    "G_MAPS_CLIENT_API_KEY": {
      "required": true
    },
    "G_MAPS_API_KEY": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
